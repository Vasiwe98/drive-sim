{
  "name": "drive-sim",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-based 3D vehicle simulation. Three.js + Cannon-es. Built with Claude Code as a 2026 rebuild of a 2014 Blender+Python project.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "cannon-es": "^0.20.0",
    "cannon-es-debugger": "^1.0.0",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "vite": "^5.4.0"
  }
}
